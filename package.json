{
  "devDependencies": {
    "markdown-spellcheck": "^1.3.1",
    "markdownlint-cli": "^0.44.0"
  },

  "scripts": {
    "test": "markdownlint **/*.md && mdspell -r --ignore-acronyms --ignore-numbers --en-us '**/*.md' '!**/node_modules/**/*.md'"
  }
}
